<Window x:Class="BeagleStreet.Net.JourneyRunner.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:BeagleStreet.Net.JourneyRunner.ViewModels"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="Beagle Street Journey Launcher" Height="350" Width="525">
    
    <Grid>

        <Grid.DataContext>
            <viewModels:MainViewModel />
        </Grid.DataContext>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0" Grid.Row="0" ItemsSource="{Binding Journeys}" SelectedItem="{Binding SelectedJourney}" DisplayMemberPath="Name"></ListBox>
        
        <StackPanel Grid.Column="0" Grid.Row="1">
            <Button Content="+" Width="25" Height="25" Command="{Binding AddJourneyCommand}"></Button>
        </StackPanel>
        
        <Grid Grid.Column="1" Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Column="0" Grid.Row="0" Text="{Binding SelectedJourney.Description}" Margin="5" TextWrapping="Wrap"></TextBlock>
            
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <TextBlock Text="Chrome" Margin="5" />
                <TextBlock Text="IE" Margin="5" />
                <TextBlock Text="FireFox" Margin="5" />
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <TextBlock Text="Environment" Height="30" Margin="5" VerticalAlignment="Center" Padding="0,5,0,0" />
                <ComboBox ItemsSource="{Binding Environments}" SelectedItem="{Binding SelectedEnvironment}" DisplayMemberPath="Name" Width="160" Height="25" Margin="5"  />
            </StackPanel>

            <TextBox Grid.Row="3" Text="{Binding SelectedEnvironment.BaseUrl}" Width="238" HorizontalAlignment="Left" Height="25" Margin="5" />

            <StackPanel Grid.Row="4" Orientation="Horizontal">
                <TextBlock Text="Time between page (milliseconds)"  Height="30" Margin="5" />
                <TextBox Width="100" Height="25"></TextBox>
            </StackPanel>
            
            <StackPanel Grid.Row="5" Orientation="Horizontal">
                <Button Content="Prev" Width="60" Height="30" Margin="5" Command="{Binding GoBackCommand}" />
                <Button Content="{Binding PauseButtonText, FallbackValue=Pause, TargetNullValue=Pause}" Width="60" Height="30" Margin="5" Command="{Binding PauseJourneyCommand}" />
                <Button Margin="5" Width="60" Height="30" Content="Stop" Command="{Binding StopJourneyCommand}"  />
                <Button Content="Next" Width="60" Height="30" Margin="5" Command="{Binding GoForwardCommand}" />
            </StackPanel>

        </Grid>

        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button HorizontalAlignment="Right" Margin="5" Width="75" Height="25" Content="Launch" Command="{Binding LaunchJourneyCommand}"  />
            <Button HorizontalAlignment="Right" Margin="5" Width="75" Height="25" Content="Stop" Command="{Binding StopJourneyCommand}"  />
        </StackPanel>
        
        
    </Grid>
</Window>



